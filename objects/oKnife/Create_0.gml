destroy_after_ts = false;

// Sprite
sprite_index = spr_knife_side;

alarm[0] = 50;

damage = 25;

direction = point_direction(x, y, mouse_x, mouse_y);
image_angle = point_direction(x, y, mouse_x, mouse_y);
